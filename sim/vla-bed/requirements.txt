# UR5e VLA sim bed — simulation-side pins (SDD §11). Python 3.13 venv, NumPy 2.
# LeRobot (0.6.0, lerobot[smolvla]) is installed separately at P2 from
# requirements/smolvla.txt; it is not needed for P0/P1 and pulls torch.
mujoco==3.10.0
mjviser==0.0.14
viser==1.1.0
mink==1.3.0
pyzmq==27.2.0
msgpack==1.1.2
numpy>=2.0
pillow>=10.0
