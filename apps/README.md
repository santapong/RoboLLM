# Applications

Runnable user-facing processes live here:

- `mcp/` exposes the ROS 2 bridge over MCP stdio.
- `dashboard/` serves the browser dashboard and its static assets.

Launch them through `scripts/launch/`. The maintained shared ROS and Gazebo
implementation lives in `src/robollm/`.
