# Checked configuration

Version-controlled experiment and runtime configuration belongs here.
`training/` contains the frozen B1 SmolVLA handoff configuration. Generated
artifacts, datasets, and checkpoints belong under ignored local directories.
